import React from "react";

function Google() {
  return (
    <Button
    type="submit"
    fullWidth
    variant="contained"
    onClick={validateInputs}
  >
    Sign in
  </Button>
  );
}

export default Google;
